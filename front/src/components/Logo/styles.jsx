import { styled } from "styled-components";

const ImageWrapper = styled.div`

display: grid;
place-items: center;

  
`;

const Photo = styled.img`

max-width: 200px;
`

export default {
    ImageWrapper,
    Photo,
};